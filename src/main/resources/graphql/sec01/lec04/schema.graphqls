type Query{
    # Fetch All Customers
    customers: [Customer]!
}

type Customer{
    id: ID
    name: String
    age: Int
    city: String
    orders: [CustomerOrder]!
}

type CustomerOrder{
    id: ID!
    description: String
}

input AgeFilter{
    minAge: Int
    maxAge: Int
}